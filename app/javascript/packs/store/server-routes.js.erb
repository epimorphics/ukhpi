/** Routes to the server actions */

export default {
  browsePath: '<%= Rails.application.config.relative_url_root %>/browse',
  assetsPath: '<%= Rails.application.config.relative_url_root %>/assets',
}
