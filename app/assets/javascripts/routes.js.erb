/** Simple routes table to export path information from Rails */

modulejs.define( "routes", [],
function() {
  return {
    explore: "<%= Rails.application.config.relative_url_root%><%= Rails.application.routes.url_helpers.explore_path %>",
    new_exploration: "<%= Rails.application.config.relative_url_root%><%= Rails.application.routes.url_helpers.new_exploration_path %>",
    public_path: "<%= Rails.application.config.relative_url_root%>/"
  }
} );
